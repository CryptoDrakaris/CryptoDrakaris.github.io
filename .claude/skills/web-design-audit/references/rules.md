# Свод правил web-дизайна

Источники:
- разделы 1–8: ui-ux-pro-max-skill (`references/quick-reference.md`, `data/ux-guidelines.csv`, `data/stacks/*.csv`), MIT License, Copyright (c) 2024 Next Level Builder. Отобраны и переписаны правила для веба;
- раздел 11: taste-skill (`skills/taste-skill/SKILL.md`, `skills/redesign-skill/SKILL.md`), MIT License, Copyright (c) 2026 Leonxlnx. Отобраны правила, не привязанные к стеку и не спорящие с каноном бренда;
- разделы 9 и 10 добавлены для этого проекта.

**Способ проверки:** [S] – `audit-static.mjs`, [B] – `browser-checks.js` или браузер вручную, [C] – чтение кода, [V] – визуально по скриншотам.

**Приоритет:** CRITICAL – блокирует использование или нарушает WCAG A/AA по ключевому контенту. HIGH – заметно ухудшает опыт или Core Web Vitals. MEDIUM – качество и последовательность. LOW – полировка.

Порядок категорий = порядок проверки.

---

## 1. Доступность

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `color-contrast` | Обычный текст ≥ 4.5:1 к фону, крупный (≥ 24px или ≥ 18.66px bold) ≥ 3:1. Считать по фактическому фону, включая карточки и полупрозрачные слои | CRITICAL если < 3:1 у основного текста, иначе HIGH | [S] токены, [B] фактически |
| `non-text-contrast` | Границы полей ввода, иконки со смыслом, индикаторы состояния ≥ 3:1 к соседнему цвету | HIGH | [B] [V] |
| `focus-states` | Видимый фокус у каждого интерактивного элемента (`:focus-visible`), контраст индикатора ≥ 3:1. `outline: none` только с заменой | CRITICAL | [S] [B] Tab |
| `focus-not-obscured` | Липкая шапка, баннеры, оверлеи не закрывают элемент в фокусе (`scroll-padding-top` ≥ высоты шапки) | HIGH | [B] |
| `keyboard-nav` | Всё доступно с клавиатуры, порядок Tab совпадает с визуальным, нет ловушек фокуса | CRITICAL | [B] Tab |
| `skip-links` | Первая фокусируемая ссылка ведёт к основному контенту | MEDIUM | [S] |
| `alt-text` | У содержательных изображений осмысленный `alt`, у декоративных `alt=""` или `aria-hidden` | CRITICAL | [S] [C] |
| `aria-labels` | У кнопок и ссылок-иконок есть доступное имя (`aria-label`, скрытый текст) | CRITICAL | [S] |
| `icon-context` | Декоративная иконка рядом с текстом скрыта (`aria-hidden="true"`), самостоятельная иконка имеет текстовую альтернативу | MEDIUM | [S] [C] |
| `heading-hierarchy` | Один `h1` на странице, уровни без пропусков (h2 → h4 нельзя) | MEDIUM | [S] |
| `landmarks` | Есть `header`, `nav` с `aria-label` при нескольких навигациях, `main`, `footer` | MEDIUM | [S] |
| `html-lang` | `lang` у `<html>` соответствует языку страницы, фрагменты на другом языке помечены `lang` | HIGH | [S] |
| `color-not-only` | Смысл не передаётся только цветом: активный пункт, ошибка, статус дублируются формой, текстом или иконкой | HIGH | [V] [C] |
| `web-target-size` | Цели указателя ≥ 24×24 CSS px или достаточный зазор (WCAG 2.5.8). Исключение – ссылки внутри текста | HIGH | [B] |
| `touch-comfort` | Основные кнопки и пункты меню на мобильном ≥ 44×44 px, зазор ≥ 8 px | MEDIUM | [B] |
| `reduced-motion` | При `prefers-reduced-motion: reduce` анимации и параллакс отключены или сведены к fade | HIGH | [S] |
| `auto-rotation-controls` | Карусели и автодвижущийся контент: пауза, остановка при фокусе и reduced motion | HIGH | [C] |
| `live-updates` | Динамически меняющиеся счётчики и результаты фильтра объявляются через `aria-live="polite"` без переноса фокуса | MEDIUM | [S] [C] |
| `zoom-reflow` | Масштаб 200% и ширина 320 px без горизонтальной прокрутки и потери контента | HIGH | [B] |
| `dragging-alternative` | Для перетаскивания есть альтернатива кнопками или клавиатурой | HIGH | [C] |

## 2. Взаимодействие

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `hover-vs-tap` | Основные действия не зависят от hover: на тач-экране всё доступно тапом | HIGH | [C] |
| `state-clarity` | Hover, focus, active, disabled, current различимы и в едином стиле | MEDIUM | [V] [C] |
| `cursor-pointer` | Кликабельные не-ссылки (`summary`, `button`, `select`, карточки) имеют `cursor: pointer` | LOW | [C] |
| `semantic-controls` | Действие – `<button>`, переход – `<a href>`. Нет `div` с `onclick` | HIGH | [S] [C] |
| `loading-feedback` | Асинхронные действия показывают состояние загрузки, кнопка блокируется | MEDIUM | [C] |
| `tap-delay` | `touch-action: manipulation` на интерактивных элементах, если есть двойной тап-зум | LOW | [C] |
| `external-links` | `target="_blank"` сопровождается `rel="noopener"`, внешний переход понятен из контекста | LOW | [S] |
| `escape-routes` | Модалки и выпадающие меню закрываются Esc, кликом вне и видимой кнопкой | HIGH | [B] |

## 3. Производительность

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `image-dimension` | У `<img>` заданы `width`/`height` или `aspect-ratio` у контейнера (CLS < 0.1) | HIGH | [S] |
| `image-optimization` | WebP/AVIF, размер файла под фактический размер отображения (не больше ~2× с учётом DPR) | HIGH | [B] |
| `lazy-load-below-fold` | `loading="lazy"` у изображений ниже первого экрана, у первого экрана – нет, у LCP-картинки `fetchpriority="high"` | MEDIUM | [S] [C] |
| `font-loading` | `font-display: swap` или `optional`, `preconnect` к хосту шрифтов, не больше 2–3 семейств и нужных начертаний | MEDIUM | [S] |
| `third-party-scripts` | Сторонние скрипты `async`/`defer`, без лишних | MEDIUM | [S] |
| `zero-js-default` | Для статических страниц JS только там, где есть интерактив. В Astro – без лишних `client:*` | MEDIUM | [C] |
| `content-jumping` | Место под асинхронный контент зарезервировано, шрифты не сдвигают раскладку заметно | MEDIUM | [V] |
| `heavy-originals` | В сетках и списках не грузятся оригиналы и видео. Видео и GIF не автопроигрываются в сетке | HIGH | [B] |

## 4. Стиль и последовательность

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `brand-consistency` | Палитра, шрифты, радиусы, тени совпадают с каноном проекта на всех страницах | MEDIUM | [V] [C] |
| `color-semantic` | Цвета через токены (CSS-переменные), без сырых hex в компонентах | MEDIUM | [S] [C] |
| `no-emoji-icons` | Иконки интерфейса – SVG из одного набора, не эмодзи. Эмодзи в тексте статей допустимы | MEDIUM | [C] |
| `icon-style-consistent` | Один стиль иконок (заливка или контур), единые размеры | LOW | [V] |
| `elevation-consistent` | Единая шкала теней и радиусов | LOW | [C] |
| `primary-action` | Одна основная кнопка (CTA) на экран, остальные визуально подчинены | MEDIUM | [V] |
| `effects-purpose` | Blur, свечение, градиенты несут роль (выделение, слой), а не только декор | LOW | [V] |
| `brand-assets` | Чужие логотипы – только официальные, без искажений. Фан- и неофициальные сайты не выдают себя за официальные | HIGH | [V] [C] |

## 5. Раскладка и адаптив

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `viewport-meta` | `width=device-width, initial-scale=1`, зум не отключён (`user-scalable=no`, `maximum-scale=1` запрещены) | CRITICAL | [S] |
| `horizontal-scroll` | Нет горизонтальной прокрутки страницы на 375 px. Широкие таблицы и код – в своём контейнере с `overflow-x: auto` | HIGH | [B] |
| `breakpoint-consistency` | Раскладка корректна на 375, 768, 1024, 1440 px, без «мёртвых» диапазонов между точками | HIGH | [B] [V] |
| `readable-font-size` | Основной текст на мобильном ≥ 16px, никакой текст < 12px | MEDIUM | [S] [B] |
| `line-length` | Длинный текст 45–75 символов в строке на десктопе, max-width у абзацев | MEDIUM | [B] |
| `container-width` | Единая максимальная ширина контейнера и боковые поля ≥ 16 px на всех страницах | MEDIUM | [V] [C] |
| `spacing-scale` | Отступы по шкале (кратные 4/8 px или единые clamp-токены), ритм секций одинаковый | LOW | [C] [V] |
| `fixed-element-offset` | Липкая шапка не перекрывает якоря и первый экран | MEDIUM | [B] |
| `viewport-units` | `100vh` на мобильном заменён на `svh`/`dvh` или ограничен | LOW | [S] |
| `z-index-management` | Шкала z-index, без 9999 | LOW | [S] |
| `long-token-wrapping` | Адреса, хеши, URL переносятся (`overflow-wrap: anywhere`) и не распирают контейнер | MEDIUM | [B] |
| `content-priority` | На мобильном главный контент и CTA на первом экране, второстепенное ниже | MEDIUM | [V] |
| `orientation-support` | Горизонтальная ориентация телефона не ломает шапку и первый экран | LOW | [B] |

## 6. Типографика и цвет

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `line-height` | Основной текст 1.5–1.75, заголовки 1.0–1.3 | MEDIUM | [S] [C] |
| `font-scale` | Последовательная шкала размеров, без случайных значений | LOW | [C] |
| `weight-hierarchy` | Заголовки 600–700, текст 400, подписи 500 | LOW | [C] |
| `font-coverage` | Шрифт поддерживает все языки сайта (кириллица, казахские ә ғ қ ң ө ұ ү һ і, диакритика). Иначе – шрифт-замена на весь язык, а не посимвольный фолбэк | HIGH | [C] [V] |
| `heading-line-balance` | Короткие заголовки `text-wrap: balance`, без висячих одиночных слов | LOW | [V] |
| `letter-spacing` | Без плотного трекинга в основном тексте. Разрядка капсом – только для коротких подписей | LOW | [C] |
| `number-tabular` | Цифры в таблицах, счётчиках, ценах – `font-variant-numeric: tabular-nums` или моноширинные | LOW | [C] |
| `color-dark-mode` | Тёмная тема: не чистый чёрный с чистым белым, приглушённые оттенки. Контраст считается отдельно для каждой темы | MEDIUM | [S] [B] |
| `muted-text` | Второстепенный серый текст всё равно ≥ 4.5:1 | HIGH | [S] [B] |
| `truncation-strategy` | Перенос лучше обрезки. Обрезка – с многоточием и доступом к полному тексту | MEDIUM | [V] |

## 7. Анимация

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `transform-performance` | Анимируются только `transform` и `opacity`, не width/height/top/left/margin | MEDIUM | [S] |
| `excessive-motion` | 1–2 ключевых анимированных элемента на экран, без бесконечных декоративных анимаций | MEDIUM | [C] [V] |
| `duration-timing` | Длительности из общих токенов, отклик на действие ощущается мгновенным (микро 100–250 ms) | LOW | [C] |
| `easing` | Появление – замедление (ease-out), уход – ускорение (ease-in), линейно – только прогресс и вращение | LOW | [C] |
| `no-blocking-animation` | Анимация не блокирует ввод и прерывается новым действием | MEDIUM | [B] |
| `layout-shift-avoid` | Hover-эффекты не сдвигают соседние элементы | MEDIUM | [V] |
| `parallax-subtle` | Параллакс минимальный и выключается при reduced motion | MEDIUM | [C] |

## 8. Навигация и формы

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `nav-state-active` | Текущий раздел выделен в навигации (`aria-current="page"` + визуально), включая вложенные страницы раздела | MEDIUM | [S] [B] |
| `navigation-consistency` | Шапка и меню одинаковые на всех страницах | MEDIUM | [V] |
| `mobile-menu` | Меню на мобильном открывается и закрывается с клавиатуры, имеет доступное имя и состояние (`aria-expanded` или `<details>`) | HIGH | [B] [C] |
| `deep-linking` | Каждая значимая страница и состояние имеют свой URL. Фильтры, если важны для шаринга, отражаются в URL | LOW | [C] |
| `back-behavior` | «Назад» возвращает на предыдущую страницу и позицию. Редиректы – через `location.replace`, не засоряют историю | MEDIUM | [B] |
| `breadcrumb-web` | При глубине 3+ уровня есть хлебные крошки или ссылка на родителя | LOW | [V] |
| `empty-states` | Пустой список или фильтр без результатов показывает понятное сообщение и выход (сброс) | MEDIUM | [B] |
| `focus-on-route-change` | При клиентской навигации (SPA, View Transitions) фокус переносится на `main` или `h1` | MEDIUM | [C] |
| `input-labels` | У каждого поля видимая подпись (`<label for>` или обёртка), не только placeholder | CRITICAL | [S] |
| `error-placement` | Ошибка рядом с полем, связана через `aria-describedby`, объясняет причину и способ исправить | HIGH | [C] |
| `input-type-keyboard` | Правильные `type` и `autocomplete` (email, tel, name) | MEDIUM | [S] |
| `submit-feedback` | После отправки – состояние загрузки, затем успех или ошибка | HIGH | [C] |
| `required-indicators` | Обязательные поля отмечены и в разметке (`required`) | MEDIUM | [S] |

## 9. SEO, мета и шаринг

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `meta-title` | Уникальный `<title>` на каждой странице, до ~60 символов | HIGH | [S] |
| `meta-description` | `meta description` на каждой индексируемой странице | MEDIUM | [S] |
| `canonical` | `link rel="canonical"` на индексируемых страницах, абсолютный URL после выбора домена | LOW | [S] |
| `open-graph` | `og:title`, `og:description`, `og:image` (абсолютный URL), `twitter:card` | MEDIUM | [S] |
| `favicon` | Есть favicon (SVG или PNG), `theme-color` в цвет фона | LOW | [S] |
| `sitemap-robots` | `sitemap.xml` и `robots.txt` после выбора домена | LOW | [S] |
| `redirect-pages` | Служебные страницы-редиректы помечены `noindex` | LOW | [S] |

## 10. Мультиязычность

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `hreflang` | Каждая языковая версия ссылается на все остальные через `link rel="alternate" hreflang`, коды ISO 639-1 (`kk`, не `kz`) | MEDIUM | [S] |
| `lang-switch` | Переключатель языка ведёт на ту же страницу в другом языке, не на главную. Названия языков – на самих языках, у ссылок `lang`/`hreflang` | MEDIUM | [B] [C] |
| `no-untranslated` | В интерфейсе нет строк на другом языке, кроме намеренных (данные, названия, термины) | MEDIUM | [V] |
| `locale-formatting` | Даты, числа, множественные формы – по локали (`Intl.*`), «6 львов», а не «6 лев» | MEDIUM | [V] [C] |
| `text-expansion` | Более длинные переводы (RU, KK обычно на 15–30% длиннее EN) не ломают кнопки, меню, заголовки | HIGH | [B] на всех языках |

## 11. Шаблонность и вкус

Правила против «собранного по шаблону» вида. Это вкус, а не поломка: приоритет не выше MEDIUM, находки этого раздела никогда не бывают CRITICAL или HIGH.

**Ограничения раздела – проверять до любой находки:**

- **Канон бренда важнее.** Если цвет, шрифт, форма или приём взяты из фирменного стиля (брендбук, CSS официального сайта, дизайн-система в CLAUDE.md), это не находка. Правила про «один акцент», насыщенность, чистый чёрный, скругления проверяются только для частей без канона.
- **Типографика языка важнее.** Тире в русском и казахском тексте грамматически обязательно – не находка. Правило `taste-dash` применяется только к английским страницам и только к заголовкам, кнопкам и подписям.
- **Стек проекта важнее.** Исправления предлагаются в терминах текущего стека. Не предлагать React, Tailwind, Motion, GSAP или библиотеки иконок, если их нет в проекте.
- **Разрешённое в CLAUDE.md важнее.** Если проект явно допускает эмодзи, мемы, разговорный регистр – это не находки.

### 11.1 Композиция и ритм

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `taste-eyebrow-overuse` | Мелкие подписи капсом над заголовками («брови», kicker, overline) – не больше одной на три секции. Вместо лишних – просто заголовок | MEDIUM | [S] |
| `taste-number-labels` | Декоративная нумерация «01 / 02 / 03», «001 · Раздел», «01 / 4» на карточках и секциях не нужна: содержимое само себе метка | LOW | [S] |
| `taste-three-cards` | Три одинаковые карточки в ряд как блок «преимущества» – главный шаблон. Альтернативы: асимметричная сетка, 2 колонки, список с крупными заголовками, горизонтальная лента | MEDIUM | [S] [V] |
| `taste-section-repetition` | Одно семейство раскладки не больше двух секций подряд (например, три сетки карточек подряд). На странице из 6+ секций – минимум 3 разных семейства | LOW | [S] [V] |
| `taste-zigzag` | Чередование «картинка слева / текст справа» – не больше двух секций подряд | LOW | [V] |
| `taste-split-header` | «Крупный заголовок слева + мелкий абзац, плавающий справа» в шапке секции – по умолчанию нет: заголовок и текст друг под другом | LOW | [V] |
| `taste-hero-overload` | В первом экране не больше 4 текстовых блоков: подпись (или ничего), заголовок до 2 строк, текст до ~20 слов, кнопки (1 основная + 1 вторичная). Статистика, логотипы, списки – отдельной секцией ниже | LOW | [S] [V] |
| `taste-hero-fit` | Заголовок, текст и основная кнопка первого экрана видны без прокрутки на 1440×900 и 375×812 | MEDIUM | [B] |
| `taste-cta-duplicate` | Одно намерение – одна формулировка на странице («Связаться» / «Написать» / «Давайте поговорим» – выбрать одну) | LOW | [C] |
| `taste-cta-wrap` | Текст кнопки на десктопе в одну строку; основная кнопка – до 3 слов | LOW | [B] |
| `taste-bento-cells` | В плиточной сетке ровно столько ячеек, сколько содержимого: без пустых ячеек-заглушек | LOW | [V] |

### 11.2 Декор и приметы

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `taste-overlay-pills` | Плашки-теги поверх изображений («Brand · 02», «PLATE 03») – по умолчанию нет. Допустимо, если плашка несёт смысл (название бренда карточки, тип медиа GIF/VIDEO) | LOW | [V] |
| `taste-status-dots` | Цветные точки перед пунктами меню, строками списков, бейджами – только если передают реальное состояние или цвет-идентификатор | LOW | [V] |
| `taste-middot` | Средняя точка «·» как универсальный разделитель – не больше одной в строке метаданных | LOW | [S] |
| `taste-deco-strips` | Декоративные полосы «DESIGN · BUILD · SHIP», погода/город/время, «Scroll ↓», версии «v0.6» на маркетинговой странице – нет | LOW | [V] [S] |
| `taste-fake-ui` | Имитация скриншота интерфейса из div-прямоугольников – нет. Реальный скриншот, изображение или ничего | MEDIUM | [C] |
| `taste-glow-gradient` | Неоновые свечения, градиентный текст в крупных заголовках, фиолетово-синий «ИИ-градиент» как фон по умолчанию – нет (если не канон бренда) | LOW | [V] |
| `taste-shape-lock` | Одна шкала скруглений на странице (всё острое / всё мягкое / кнопки-таблетки + единый радиус карточек), и она соблюдается везде | LOW | [V] [C] |
| `taste-shadow-tint` | Тени подкрашены под цвет фона, не чистый чёрный с прозрачностью на светлом фоне (кроме жёстких графичных теней из канона) | LOW | [C] |
| `taste-theme-island` | Внезапная тёмная секция посреди светлой страницы (или наоборот) без системы – выглядит как ошибка копирования | LOW | [V] |
| `taste-accent-lock` | Один акцентный цвет на страницу; не появляется «чужой» акцент в отдельной секции (кроме случаев, где цвет – идентификатор бренда или коллекции) | LOW | [V] |
| `taste-icon-consistency` | Иконки одного семейства и одной толщины линии; иконки-клише (ракета = «запуск», щит = «безопасность») заменены | LOW | [V] |

### 11.3 Типографика

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `taste-font-default` | Шрифт без характера по умолчанию (Inter/системный везде) при отсутствии канона – стоит заменить на шрифт с характером | LOW | [C] |
| `taste-serif-default` | Засечки «потому что премиально» без причины в бренде – нет. Акцентное слово в заголовке – курсив или жирный того же шрифта, а не вставка другого семейства | LOW | [V] |
| `taste-italic-descender` | Курсив в крупных заголовках с `line-height` ≤ 1 обрезает выносные (y g j p q) – межстрочный ≥ 1.1 | LOW | [B] |
| `taste-caps-everywhere` | Все подзаголовки капсом – монотонно; капс только для коротких меток | LOW | [V] |
| `taste-dash` | **Только английские страницы:** длинное тире «—» и тире-разделитель «–» в заголовках, кнопках, подписях заменить на точку, запятую, двоеточие или дефис. В RU/KK не применяется | LOW | [S] |
| `taste-self-host-fonts` | Шрифты хранятся на своём домене (`@font-face` + `font-display: swap`), а не подключаются ссылкой на Google Fonts: быстрее первая отрисовка и данные посетителей не уходят третьей стороне | LOW | [S] |

### 11.4 Контент

| ID | Правило | Пр. | Проверка |
|----|---------|-----|----------|
| `taste-placeholder` | Никаких «Lorem ipsum», «John Doe», «Acme», «Иван Иванов», «Компания N» в опубликованном тексте | MEDIUM | [S] |
| `taste-fake-numbers` | Подозрительно круглые демо-числа (99.99%, 1234567, $100.00) в опубликованном тексте – проверить, реальные ли | LOW | [C] |
| `taste-copy-cliche` | Штампы ИИ-копирайта. EN: elevate, seamless, unleash, next-gen, game-changer, delve, tapestry, revolutionize, «in the world of». RU: «погрузитесь в мир», «откройте для себя», «бесшовный», «инновационный», «уникальный опыт», «новый уровень» | LOW | [S] |
| `taste-poetic-labels` | Поэтичные ярлыки вместо функциональных («Полевые заметки», «На рабочем столе», «From the field») – заменить на прямые («Блог», «Отзывы») | LOW | [V] |
| `taste-dead-link` | Кнопки и ссылки на «#» – либо реальный адрес, либо элемент неактивен | MEDIUM | [S] |
| `taste-empty-states` | Пустые состояния (ничего не найдено, нет записей) оформлены и подсказывают, что делать | LOW | [B] |

---

## Стековые заметки: Astro

- Статические компоненты – `.astro` без `client:*`. Интерактив – небольшим `<script>` или островом с минимальной директивой (`client:visible`, `client:idle`).
- Контент – content collections со схемой.
- `set:html` – только для доверенного или санированного содержимого.
- Изображения из `src/` – через `astro:assets` (`<Image />`, `<Picture />`); файлы из `public/` отдаются как есть, оптимизация – на этапе подготовки.
- Метаданные – в общем компоненте layout или head, canonical через `Astro.site`.
- `trailingSlash` согласован со ссылками, иначе лишние редиректы.
- При `ClientRouter` (View Transitions) контент должен работать и без JS.
